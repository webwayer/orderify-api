{
  "name": "orderify",
  "version": "0.0.0",
  "repository": "https://github.com/webwayer/orderify-api",
  "author": "Rodion Balandin <webwayer@gmail.com>",
  "license": "none",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "setup_storage": "rm -Rf storage; mkdir storage",
    "prestart": "yarn setup_storage",
    "lint": "tslint 'packages/**/*.ts'",
    "lint_fix": "tslint 'packages/**/*.ts' --fix",
    "start": "ts-node packages/app/src/server.ts"
  },
  "dependencies": {
    "lerna": "^3.16.4",
    "ts-node": "^8.4.1",
    "tslint": "^5.20.0",
    "typescript": "^3.6.2"
  }
}
